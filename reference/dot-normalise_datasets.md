# Resolve the `datasets` argument to a clean alias vector

Resolve the `datasets` argument to a clean alias vector

## Usage

``` r
.normalise_datasets(datasets)
```

## Arguments

- datasets:

  User-supplied aliases (or `NULL`/`"all"`).

## Value

A `character` vector of recognised aliases (de-duplicated, order
preserved).
