# Resolve the 'stat' argument to a clean vector

Resolve the 'stat' argument to a clean vector

## Usage

``` r
.normalise_stat(stat)
```

## Arguments

- stat:

  User-supplied statistics for SLGA datasets (or `NULL`/`"all"`).

## Value

A `character` vector of valid statistics (de-duplicated, order
preserved).
