# Fix improper API keys

Fix improper API keys

## Usage

``` r
.check_api_key(api_key)
```

## Arguments

- api_key:

  A `string` value containing a TERN API key for checking

## Value

A `string` value with replacement of troublesome characters if
necessary.
