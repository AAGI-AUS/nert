# Check that the user hasn't blindly copied the "your_api_key" string from the examples

Check that the user hasn't blindly copied the "your_api_key" string from
the examples

Check that the user hasn't blindly copied the "your_api_key" string from
the examples

## Usage

``` r
.check_not_example_api_key(.api_key)

.check_not_example_api_key(.api_key)
```
