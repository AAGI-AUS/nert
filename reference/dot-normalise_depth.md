# Resolve the 'depth' argument to a clean vector

Resolve the 'depth' argument to a clean vector

## Usage

``` r
.normalise_depth(depth)
```

## Arguments

- depth:

  User-supplied soil depths (or `NULL`/`"all"`).

## Value

A `character` vector of valid soil depths (de-duplicated, order
preserved).
