# SLGA metadata: version, suffix, date for each soil attribute

SLGA metadata: version, suffix, date for each soil attribute

## Usage

``` r
.SLGA_META
```

## Format

An object of class `list` of length 8.
