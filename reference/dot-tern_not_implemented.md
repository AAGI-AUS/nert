# Emit an informative error for unsupported dataset IDs

Emit an informative error for unsupported dataset IDs

## Usage

``` r
.tern_not_implemented(dataset_id)
```

## Arguments

- dataset_id:

  The raw dataset ID supplied by the user.
