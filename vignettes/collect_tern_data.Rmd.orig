---
title: "collect_tern_data() for bulk TERN dataset collection"
author: "Max Moldovan and Russell Edson"
output:
  rmarkdown::html_document
vignette: >
  %\VignetteIndexEntry{collect_tern_data}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
  %\VignetteDepends{terra}
---

<!-- STOP! Do not edit this file. This file is generated from collect_tern_data.Rmd.orig -->

```{r knitrsetup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.width = 7,
  fig.height = 7,
  fig.align = "center",
  fig.path = "vignettes/"
)
```

The `collect_tern_data()` function provides a simple way to extract values from one or more TERN datasets over multiple locations and dates. And unlike the individual dataset functions (`read_smips()`, `read_asc()`, etc) which return `SpatRaster` objects, this function returns an analysis-ready `data.table` object with the extracted point data values.

## Basic usage of collect_tern_data()

As an example, we can use `collect_tern_data()` to extract all of the SMIPS soil moisture datasets for a single location across multiple dates:

```{r collect-smips}
library(nert)

dt <- collect_tern_data(
  date_range = seq(as.Date("2024-01-01"), as.Date("2024-01-04"), by = "day"),
  lon = 138.6,
  lat = -34.9,
  datasets = "SMIPS",
  verbose = TRUE
)
head(dt)
```

When `verbose = TRUE` (default), a list of the datasets that will be collected is generated in the output, together with progress messages as the data points are downloaded.

By default, `collect_tern_data()` extracts _all available dataset variants_ unless you specify a smaller subset. For instance, in the above code, we collect all of the available SMIPS datasets: `"totalbucket"`, `"SMindex"`, `"bucket1"`, `"bucket2"`, `"deepD"`, and `"runoff"`. Extracting the other datasets works similarly. If we use `collect_tern_data()` to download data for the SLGA available water capacity dataset `"AWC"`, by default it will download the data for all available depths, as well as all three statistics (the estimated value `EV`, plus the `05` and `95` percentile bounds, which together span a 90% interval):

```{r collect-awc}
dt <- collect_tern_data(
  dates = c("2020-01-01"),
  lon = 138.6,
  lat = -34.9,
  datasets = "AWC",
  verbose = FALSE
)
names(dt)
```

This ensures that, by default, you get the most comprehensive data for each dataset. The arguments for `collect_tern_data()` can always be used to specify variants where desired. For example, if we only wanted the estimated value `EV` at 0-5cm and 5-15cm (i.e., depths `000_005` and `005_015`), we can specify those with the `stat` and `depth` arguments:

```{r collect-awc-spec}
dt <- collect_tern_data(
  dates = c("2020-01-01"),
  lon = 138.6,
  lat = -34.9,
  datasets = "AWC",
  stat = "EV",
  depth = c("000_005", "005_015"),
  verbose = FALSE
)
names(dt)
```


## Efficiently download data points at multiple spatial locations

You can use `collect_tern_data()` to extract point values for multiple spatial locations, each provided by a longitude/latitude pair. The function will perform the bulk download across spatial locations efficiently, making streaming data from the fetched COGs _only once_, regardless of the number of locations you have requested.

For example, the below R code extracts SMIPS `"SMindex"` data and canopy height information across three locations and a range of dates. The output `data.table` will contain one row per location/date combination.

```{r collect-spatial-locs}
dt_multi <- collect_tern_data(
  lon = c(138.6, 139.5, 140.1),
  lat = c(-34.9, -35.2, -34.5),
  date_range = c("2024-01-01", "2024-01-05"),
  datasets = c("SMIPS", "CANOPY"),
  smips_collection = "SMindex",
  verbose = FALSE
)
head(dt_multi)
```

For convenience, you can also provide the longitude and latitude coordinates as columns `lon`, `lat` inside a `data.frame`. You can then pass this to the `xy` argument in the `collect_tern_data()` call:

```{r collect-spatial-xy}
locations <- data.frame(
  lon = c(138.6, 139.5, 140.1),
  lat = c(-34.9, -35.2, -34.5)
)

dt_xy <- collect_tern_data(
  xy = locations,
  date_range = "2024-01-15",
  datasets = c("ASC"),
  asc_collection = "EV",
  verbose = FALSE
)
dt_xy
```


## Graceful handling of failed dataset fetches

If the download for one of the datasets fails (e.g., due to a network error), that column will simply contain `NA` for the affected rows. The function will then continue downloading the other datasets as normal. That is, download failures are handled gracefully: a download error for one of the datasets does not stop the entire call.

You can specify `na.rm = TRUE` in the `collect_tern_data()` call, which will remove rows where all of the dataset columns were `NA`.
